# Tenant

Get the list of tenants with related policies count. Use specific policy commands with '--tenant' filtering rule to get the per-tenant policy details.

```
DOC_TEMPLATE:get_aci_tenant.all:iserver.output.default
```

[[Back]](./README.md)