apiVersion: "k8s.cni.cncf.io/v1"
kind: NetworkAttachmentDefinition
metadata:
  namespace: ${NAMESPACE}
  name: ${NAME}-net1
spec:
  config: '{
            "cniVersion": "0.3.1",
            "name": "${NAME}-net1",
            "type": "macvlan",
            "master": "${MACVLAN_MASTER}",
            "mode": "bridge",
            "ipam": {
              "type": "static",
              "addresses": [
                {
                  "address": "${NET1_BRIDGE_IP}/${NET1_PREFIX}"
                }
              ]
            }
          }'
