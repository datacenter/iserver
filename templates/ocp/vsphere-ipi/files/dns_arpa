$TTL    604800
@       IN      SOA     ${OCP_BASE_DOMAIN}. root.${OCP_BASE_DOMAIN} (
      ${DNS_SERIAL}     ; Serial
             604800     ; Refresh
              86400     ; Retry
            2419200     ; Expire
             604800     ; Minimum
)
                 NS     ${OCP_BASE_DOMAIN}.

${OCP_API_VIP_OCTET_4}               PTR    api.${OCP_NAME}.${OCP_BASE_DOMAIN}.
